@model Art_fundingV0.ViewModels.LoginViewModel
@{
    Layout = null;
}
@if (Model.LoggedIn)
{
    <h3>
        Vous êtes déjà authentifié avec le login :
        @Model.adresse_emailUE
    </h3>
    @Html.ActionLink("Voulez-vous vous déconnecter ?", "Deconnexion")

}
else
{
    <p>
        Veuillez vous authentifier :
    </p>
    using (Html.BeginForm())
    {
        <div>
           @Html.Label("veuillez entrer ")
            @*@Html.LabelFor(m => m.entreprise.adresse_emailUE)*@
           
           @Html.TextBoxFor(m => m.entreprise.adresse_emailUE)
            @Html.ValidationMessageFor(m => m.entreprise.adresse_emailUE)
        </div>
        <div>
            @Html.LabelFor(m => m.entreprise.mot_de_passe)
            @Html.PasswordFor(m => m.entreprise.mot_de_passe)
            @Html.ValidationMessageFor(m => m.entreprise.mot_de_passe)
        </div>
        <input type="submit" value="Se connecter" />
        <br />
        @Html.ActionLink("Créer un compte", "CreerCompte")
    }
}
